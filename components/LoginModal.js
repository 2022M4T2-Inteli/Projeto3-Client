export const LoginModal = ({
  isOpened, closeModal
}) => {
  return isOpened ? (
    <div className="z-50 absolute modal-content bg-white w-full rounded-t-xl shadow-lg py-8 text-left px-6 inset-0 h-1/3 bottom-0 mb-8">
1231231
    </div>
  ) : null;
}